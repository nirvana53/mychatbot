# 결산 챗봇 프로그램 PRD

- 문서 버전: v1.3
- 작성일: 2026-08-10
- 작성자: (본부 담당자명 기입)
- 상태: 구현 완료(수정 2차 보완), 내부 AI agent 연동 전

## 수정 이력

| 버전 | 주요 변경 |
|---|---|
| v1.0 | 최초 버전. 단일 계정(구분+계정명) 조회, 단일 시점/추이/비교/계정목록 응답 |
| v1.1 (수정 1차) | 계정을 3단계 트리(대분류-중분류-세부계정)로 재구성, 데이터를 법인 20개+연결조정 구조의 연결결산으로 재구성(법인별 드릴다운 가능), 질문 인식을 상대 표현·느슨한 트리거로 유연화, 추이 응답에 그래프 추가, 답변마다 드릴다운 추천 질문 제공 |
| v1.2 (수정 2차) | 단일 시점 조회에 전분기/전년동기 증감 자동 표시, 미해결 질문 로그 뷰어, 조회 결과 CSV 다운로드, 재무비율 지표(영업이익률/부채비율/ROE 등) 추가, 두 시점 비교 시 세부계정 기여도 breakdown, 오타·띄어쓰기 완화 매칭, 법인별 이상치 탐지, 다중 법인 동시 비교, 기간 범위 지정 추이, 사이드바 FAQ 버튼, 로그에 사용자(이름/사번) 식별자 추가 |
| v1.3 (수정 2차 보완) | 실사용 로그에서 발견된 미해결 질문 3종을 보완: 계정명을 생략한 후속 비교 질문("2025년 1분기랑 비교해줘")이 직전 답변의 계정을 이어받도록 지원, 특정 계정명 없이 "이상치 계정"을 물으면 전체 세부계정 중 전분기 대비 변동이 큰 계정을 탐지, "새로 생긴 계정"을 물으면 직전 분기에는 없던 세부계정을 탐지 |

## 1. 배경 및 문제 정의

본부에서는 분기마다 결산 자료(엑셀)를 만들지만, 특정 계정의 수치나 분기별 추이를 확인하려면 매번 엑셀 파일을 열어 직접 찾아야 한다. 담당자가 자리에 없거나 파일 위치를 모르면 조회가 지연된다. 결산 자료는 보안 자료이기 때문에 외부 SaaS나 클라우드 LLM에 올릴 수 없고, 별도 예산도 없다.

이 문제를 해결하기 위해, 로컬 PC에 저장된 결산 데이터를 기반으로 자연어 질문에 답하는 내부용 챗봇을 만든다. 외부로 데이터가 나가지 않고, 추가 비용 없이 동작하며, 같은 본부 인원만 접속할 수 있도록 설계한다. 결산 데이터는 연결결산 성격을 가지므로(법인 20개 개별 실적 + 연결조정), 챗봇도 연결 기준 조회와 법인별 드릴다운을 함께 지원해야 한다. 수정 2차에서는 단순 조회를 넘어, 증감의 방향/크기/원인을 스스로 설명하고 이상 신호를 짚어주는 방향으로 확장했다.

## 2. 목표

- 결산 수치를 엑셀을 열지 않고 채팅으로 즉시 조회할 수 있게 한다.
- 데이터가 회사 내부망을 벗어나지 않도록 한다(외부 API/클라우드 미사용).
- 예산 투입 없이(무료 오픈소스 구성) 운영한다.
- 지금은 연결할 수 없는 회사 내부 AI agent LLM을, 나중에 코드 대부분을 바꾸지 않고 꽂아 넣을 수 있는 구조로 만든다.
- 고정된 질문 형식을 외우지 않아도, 자연스러운 표현(상대적 기간 표현, 줄임말, 오타/띄어쓰기 변형 등)으로 질문할 수 있게 한다.
- 연결 기준 수치뿐 아니라 법인별·세부계정별로 파고들어(드릴다운) 확인할 수 있게 한다.
- 단순 수치 조회에서 그치지 않고, 전기 대비 변화 방향·원인 세부계정·이상치 법인까지 한 번의 질문으로 파악할 수 있게 한다.
- 조회한 결과를 다른 문서로 옮겨 쓸 수 있게(CSV) 하고, 누가 어떤 질문을 했는지/어떤 질문을 이해하지 못했는지 담당자가 확인할 수 있게 한다.

### 성공 기준(수정 2차 기준)

- 본부 인원이 브라우저로 접속해 "이번분기 영업이익 알려줘"처럼 자유로운 표현으로 물어도 정확한 수치로 답을 받는다.
- 연결 기준 답변에서 특정 법인 실적이나 세부 계정으로 바로 이어서 물어볼 수 있고(추천 질문 클릭 또는 직접 입력), 추이 질문에는 그래프가 함께 나온다.
- 단일 시점 조회 답변에는 전분기·전년동기 대비 증감이 자동으로 붙어, 별도로 비교 질문을 하지 않아도 방향을 알 수 있다.
- 두 시점을 비교하면 어떤 세부계정이 변동을 가장 크게 만들었는지도 함께 안다.
- "매출 이상치 있는 법인 알려줘"처럼 물으면 평균에서 크게 벗어난 법인을 자동으로 짚어준다.
- "법인01이랑 법인02 비교해줘"처럼 법인을 직접 여러 개 지정해 나란히 비교할 수 있다.
- "2024년 1분기부터 2025년 2분기까지" 같은 기간 범위를 지정해 추이를 볼 수 있다.
- 담당자가 새 분기 결산 파일을 `data/raw`에 넣고 버튼 한 번으로 최신 데이터를 반영할 수 있다.
- 비밀번호를 모르는 외부/타 부서 인원은 접속해도 데이터를 볼 수 없고, 접속한 인원은 이름/사번을 남겨 질의 기록에 누가 물었는지가 남는다.
- 담당자가 사이드바에서 챗봇이 이해하지 못한 질문 목록을 확인해 파서 개선에 활용할 수 있다.

## 3. 범위(Scope)

### 포함(In scope)
- 분기별 연결결산 엑셀/CSV 파일(법인별 + 연결조정)을 읽어 조회 가능한 형태로 저장
- 3단계 계정트리(대분류-중분류-세부계정) 및 매출총이익/영업이익/당기순이익 등 계산 지표, 영업이익률/부채비율/ROE 등 비율 지표 조회
- 채팅 UI를 통한 자연어 질문 응답(단일 조회, 추이, 두 시점 비교, 다중 법인 비교, 법인별 브레이크다운, 세부계정 브레이크다운, 법인별 이상치 탐지, 계정 목록 안내)
- 절대 기간(2024년 3분기)뿐 아니라 상대 기간 표현(이번분기/작년/지난분기), 기간 범위 표현("~부터 ~까지") 인식
- 오타·띄어쓰기 변형에 대한 완화된(fuzzy) 계정/법인명 매칭
- 단일 시점 조회 시 전분기(QoQ)·전년동기(YoY) 증감 자동 표시
- 두 시점 비교 시 변동에 가장 크게 기여한 세부계정 목록 제시
- 추이 질문에 대한 그래프(라인차트), 브레이크다운/법인비교 질문에 대한 그래프(바차트)
- 조회 결과를 CSV로 내려받는 기능
- 답변마다 드릴다운 개념의 추천 질문 제시(클릭 시 바로 재질문), 사이드바 FAQ 버튼으로 자주 쓰는 질문 즉시 실행
- 접속 시 비밀번호 + 이름/사번 입력을 통한 최소한의 접근 제한 및 질의 로그 식별
- 담당자용 "최근 미해결 질문" 로그 뷰어
- LLM 백엔드를 교체 가능하게 만드는 어댑터 구조

### 미포함(Out of scope, 수정 2차 기준)
- 회사 내부 AI agent LLM 실제 연동(연동 지점만 마련)
- SSO/사번 기반 정식 인증(사번은 로그 식별용으로만 입력받으며, 검증하지 않음), 사용자별 권한 분리
- 결산 자료 수정/입력 기능(조회 전용)
- 다른 본부/부서 데이터 접근, 본부 간 데이터 비교
- 모바일 전용 UI
- 여러 계정을 한 화면에 나란히 비교하는 소형 다중 차트(small multiples)
- "올해 상반기 중 가장 높았던 분기" 같은 복잡한 조건문·원인 추론 질문(규칙 기반 파서의 한계, 내부 AI agent 연동 후 개선 예정)

## 4. 대상 사용자 및 사용 범위

- **대상 사용자**: 같은 본부 소속 인원(회계/경영기획 등 결산 데이터를 참고하는 실무자)
- **사용 환경**: 회사 내부망(사내 LAN)에서만 접속. 인터넷 구간으로 노출되지 않음.
- **접근 제어**: 본부 공용 비밀번호 1개 + 로그인 시 입력한 이름/사번(검증 없이 로그 식별용으로만 사용, 추후 사번 기반 인증으로 고도화 가능)

## 5. 주요 기능

| 기능 | 설명 | 구현 상태 |
|---|---|---|
| 결산 데이터 적재 | `data/raw`의 엑셀/CSV(법인별+연결조정, 3단계 계정트리)를 읽어 SQLite DB로 변환 | 완료 |
| 단일 시점 조회 | "이번분기 영업이익 알려줘" 같은 질문에 연결 기준 수치 응답 | 완료 |
| 전분기/전년동기 증감 자동 표시 | 단일 시점 조회 답변에 QoQ/YoY 증감·증감률을 자동으로 덧붙임 | 완료 |
| 법인별 조회(드릴다운) | 질문에 법인명을 넣으면 해당 법인만 필터링해 응답 | 완료 |
| 다중 법인 비교 | "법인01이랑 법인02 비교해줘"처럼 법인을 2개 이상 지정하면 나란히 비교 | 완료 |
| 계산 지표 조회 | 매출총이익/영업이익/당기순이익 등을 대분류 합계 공식으로 즉시 계산 | 완료 |
| 재무비율 지표 조회 | 영업이익률/매출원가율/부채비율/ROE/ROA를 계산 지표 비율로 즉시 계산 | 완료 |
| 추이 조회 + 그래프 | 특정 계정/지표의 분기별 시계열을 텍스트 + 라인차트로 응답 | 완료 |
| 기간 범위 지정 추이 | "2024년 1분기부터 2025년 2분기까지"처럼 범위를 지정해 그 구간만 추이 조회 | 완료 |
| 시점 비교 | 두 분기 간 수치 차이/증감률 응답(과거→최근 순으로 정렬해 방향 왜곡 방지) | 완료 |
| 비교 시 세부계정 기여도 | 두 시점 비교 시, 변동을 가장 크게 만든 세부계정을 기여도 순으로 함께 제시 | 완료 |
| 법인별 브레이크다운 + 그래프 | "영업이익 법인별로 보여줘" 같은 질문에 법인별 기여도를 텍스트 + 바차트로 응답 | 완료 |
| 세부계정 브레이크다운 + 그래프 | "판관비 세부적으로 보여줘" 같은 질문에 하위 계정 분해를 텍스트 + 바차트로 응답 | 완료 |
| 법인별 이상치 탐지 | "매출 이상치 있는 법인 알려줘"처럼 물으면 평균 대비 표준편차 기준으로 벗어난 법인을 제시 | 완료 |
| 상대 기간 표현 인식 | 이번분기/저번분기/작년/올해 등을 최신 데이터 기준으로 해석 | 완료 |
| 유연한 질문 트리거 | "비교해줘" 같은 고정 키워드 없이도 기간 2개가 언급되면 비교로 인식하는 등 트리거 확대 | 완료 |
| 오타/띄어쓰기 완화 매칭 | 계정명·법인명에 띄어쓰기가 다르거나 한 글자 오타가 있어도 인식 | 완료 |
| 드릴다운 추천 질문 | 답변마다 맥락에 맞는 후속 질문 2~3개를 버튼으로 제시, 클릭 시 재질문 | 완료 |
| 사이드바 FAQ 버튼 | 자주 쓰는 질문을 사이드바 버튼으로 고정 노출, 클릭 시 즉시 질문 | 완료 |
| 조회 결과 CSV 다운로드 | 답변에 딸린 표(raw rows)를 CSV 파일로 내려받기 | 완료 |
| 계정 목록 안내 | 대분류/계산지표/비율지표 및 법인 목록 안내 | 완료 |
| 데이터 재적재 | 사이드바 버튼으로 앱을 끄지 않고 최신 파일 반영 | 완료 |
| 접속 비밀번호 + 사용자 식별 | 공용 비밀번호와 이름/사번 입력으로 접근 제한 및 로그 식별 | 완료 |
| 질의 응답 로그 | 질문/답변/사용자/처리결과(resolved·unresolved)를 로컬 CSV로 기록(감사용, 외부 전송 없음) | 완료 |
| 미해결 질문 로그 뷰어 | 사이드바에서 챗봇이 이해하지 못한 최근 질문 목록을 담당자가 확인 | 완료 |
| 후속 비교 질문(계정명 생략) | 직전 답변에서 다루던 계정을 기억해, "2025년 1분기랑 비교해줘"처럼 계정명을 생략해도 이어서 비교 | 완료 |
| 계정 단위 이상치 탐지 | 특정 계정명 없이 "이상치 계정"을 물으면 전체 세부계정 중 전분기 대비 변동이 유독 큰 계정을 탐지 | 완료 |
| 신규 발생 계정 조회 | "새로 생긴 계정"을 물으면 직전 분기에는 없었는데 이번 분기에 처음 실적이 생긴 세부계정을 탐지 | 완료 |
| LLM 어댑터 구조 | Mock LLM ↔ 내부 AI agent LLM 교체 가능한 인터페이스 | 완료(내부 agent 연동은 자리표시자) |
| 내부 AI agent 실제 연동 | 사내 LLM API로 더 자연스럽고 복잡한 질의 처리 | 대기(API 접근 가능 시 진행) |

## 6. 사용자 시나리오

1. 담당자가 분기 마감 후 연결결산 엑셀(법인별+연결조정 포함)을 `data/raw` 폴더에 넣는다.
2. 앱 사이드바에서 "결산 데이터 다시 적재" 버튼을 누른다.
3. 본부 직원이 사내망에서 앱 주소로 접속해 이름/사번과 비밀번호를 입력한다.
4. 채팅창에 "이번분기 영업이익 알려줘"를 입력하면 연결 기준 수치와 전분기·전년동기 대비 증감을 함께 받는다.
5. 답변 아래 제시된 추천 질문("영업이익 법인별로 보여줘")을 클릭해 법인별 기여도를 그래프로 확인한다.
6. "매출 이상치 있는 법인 알려줘"를 입력해 평균에서 크게 벗어난 법인을 확인한다.
7. "작년 4분기랑 이번분기 매출 비교해줘"를 입력하면 증감뿐 아니라 어떤 세부계정이 변동을 주도했는지도 함께 확인한다.
8. 필요하면 답변에 딸린 CSV 다운로드 버튼으로 결과를 엑셀로 옮긴다.
9. "매출 추이 보여줘"를 입력하면 분기별 흐름을 텍스트와 라인차트로 함께 확인한다.
10. 질문/답변/작성자가 로컬 로그 파일에 기록된다(외부 전송 없음). 담당자는 사이드바에서 최근 미해결 질문을 확인해 자주 놓치는 표현을 파악한다.

## 7. 시스템 아키텍처

```
사용자(본부 내부망) → 브라우저
        ↓ (내부 IP:포트, 이름/사번 + 비밀번호 인증)
   Streamlit 앱 (app.py) ── 차트 렌더링(charts.py, Altair) / 추천 질문·FAQ 버튼 / CSV 다운로드 / 미해결 로그 뷰어
        ↓
   챗봇 오케스트레이터 (chatbot.py)
        ├─ 질문 파서 (query_engine/nlu.py)
        │     절대/상대 기간, 기간 범위("~부터~까지"), 법인명(단일/다중),
        │     3단계 계정/계산지표/비율지표, 오타·띄어쓰기 완화 매칭, 의도(단일/추이/
        │     비교/다중법인비교/법인별 브레이크다운(+이상치)/세부계정 브레이크다운/목록) 추출
        ├─ 조회 엔진 (query_engine/query.py)
        │     entity 필터 없음=연결(법인 20개+연결조정 합산), 특정 법인=드릴다운
        │     계산 지표는 대분류 합계 공식, 비율 지표는 계산지표/대분류의 비율(x100%)로 즉시 계산
        │     QoQ/YoY 증감, 세부계정 기여도 diff, 법인별 평균·표준편차 기반 이상치 계산
        ├─ LLM 어댑터 (llm/*) : 조회 결과 → 자연어 문장
        │     ├─ 현재: MockLLMClient (오프라인, 규칙 기반)
        │     └─ 추후: InternalAgentLLMClient (사내 AI agent API)
        └─ 차트 데이터 / CSV용 표 데이터 / 드릴다운 추천 질문 구성
   데이터 계층
        ├─ data/raw/*.xlsx, *.csv  : 연결결산 원본 파일(법인별+연결조정, 3단계 계정트리)
        ├─ data/finance.db (SQLite) : entity/level1/level2/level3 구조로 적재된 조회용 DB
        └─ data/query_log.csv : timestamp/user/question/answer/status(resolved·unresolved) 감사 로그
```

- 모든 컴포넌트가 로컬 PC 내부에서 동작하며, 외부 네트워크 호출은 없음(Mock LLM 기준).
- LLM 백엔드 교체는 `config.py`의 `ACTIVE_LLM_BACKEND` 값 변경 + `llm/internal_agent_client.py`의 요청/응답 처리 구현만으로 완료되도록 설계.
- 추천 질문·FAQ 버튼은 별도 처리 로직 없이 동일한 질문 파서 경로로 재입력되는 일반 텍스트 질문이다(클릭 = 타이핑).
- CSV 다운로드는 답변 텍스트/차트와 별개로 만들어지는 raw 표 데이터(`chatbot._build_table()`)를 그대로 내려주는 것이라, LLM 어댑터를 교체해도 영향받지 않는다.

## 8. 데이터 요구사항

- **입력 형식**: 엑셀(.xlsx) 또는 CSV. 시트/열에 "법인", "대분류", "중분류", "세부계정", "금액"에 해당하는 컬럼이 필요(유사어도 자동 인식, 법인 컬럼이 없으면 연결 수치로 간주).
- **파일명 규칙**: 연도(4자리)와 분기(1~4) 정보가 파일명에 포함되어야 함. 예: `2024_3Q_결산.xlsx`, `2024년 3분기 결산.xlsx`.
- **계정 구조**: 대분류(예: 매출) → 중분류(예: 제품매출) → 세부계정(예: 국내제품매출)의 3단계 트리. 매출총이익/영업이익/당기순이익 등은 원본에 별도 행으로 존재하지 않고 대분류 합계로 조회 시점에 계산됨. 영업이익률/부채비율/ROE 등 비율 지표는 계산 지표(또는 대분류) 두 개의 비율로 조회 시점에 추가로 계산됨(원본에도 저장되지 않음).
- **법인 구조**: 법인 20개(법인01~법인20)의 개별 실적 + 연결조정(내부거래 제거 등) 1건으로 구성. "법인 20개 합산 + 연결조정 = 연결결산" 관계가 성립해야 하며, 연결 수치는 별도 행으로 저장하지 않고 항상 이 합산으로 계산됨. 법인별 이상치 탐지는 연결조정을 성격이 다른 잔차로 보고 평균/표준편차 계산에서 제외함.
- **저장소**: SQLite 단일 테이블(`financial_statements`) — 연도, 분기, 보고서 구분(손익/재무상태), 법인(entity), 대분류/중분류/세부계정, 금액, 단위, 원본 파일명, 적재 시각.
- **데이터 갱신**: 적재 시 테이블을 새로 생성하고 재적재(항상 `data/raw`의 최신 파일 기준).
- **질의 로그**: `data/query_log.csv`에 시각, 로그인 시 입력한 이름/사번, 질문, 답변, 처리 결과(resolved/unresolved)를 누적 기록. 로그인 시 입력값은 검증하지 않으므로 감사 목적의 참고 정보로만 사용.

## 9. 비기능 요구사항

- **보안**: 데이터는 로컬/사내망을 벗어나지 않음. 앱은 사내망 내에서만 접근 가능하도록 방화벽 설정 필요(외부 포트포워딩 금지). 접속에는 공용 비밀번호와 이름/사번 입력이 필요.
- **비용**: 전 구성 요소(Python, Streamlit, SQLite, pandas, Altair)가 무료 오픈소스. 별도 서버/클라우드 비용 없음.
- **가용성**: 앱을 실행 중인 PC와 터미널이 켜져 있는 동안만 다른 사용자가 접속 가능(상시 서버 아님).
- **확장성**: LLM 백엔드, 데이터 소스 파싱 규칙, 계정트리/법인 구성, 계산·비율 지표는 코드 수정 없이(또는 최소 수정으로) 교체·확장 가능하도록 어댑터/규칙 기반 매칭으로 분리.

## 10. 제약사항 및 가정

- 회사 내부 AI agent API 접근 정보(엔드포인트, 인증, 모델명)가 아직 없어 실제 연동은 보류 상태이며, 현재는 규칙 기반 Mock 응답으로 대체함.
- 현재 데이터는 검증용 가상 결산 데이터(법인 20개+연결조정, 6개 분기)이며, 실제 결산 자료로 교체가 필요함.
- 질문 인식은 여전히 규칙 기반(정규식/키워드/오타 완화 매칭)이라, 표현이 다양해졌어도 아주 복잡한 조건문, 원인 추론("왜 늘었어?" 수준의 자유 서술), 완전히 새로운 표현은 놓칠 수 있음(내부 AI agent 연동 시 개선 예정).
- 오타 허용 매칭은 유사도(ratio) 임계값 기반이라, 서로 다른 계정명이 우연히 비슷하면 잘못 매칭될 가능성이 있음(응답에 항상 실제로 인식한 계정/법인명을 명시해 오인식을 바로 알아챌 수 있게 완화).
- 이상치 탐지(법인 단위/계정 단위 모두)는 단순 평균/표준편차 기반이라, 표본이 적거나(3개 미만) 분포가 특이하면 통계적으로 유의미하지 않을 수 있음.
- 후속 비교 질문("2025년 1분기랑 비교해줘")의 계정 이어받기는 세션 내 마지막으로 조회에 성공한 계정/법인/시점 1개만 기억하며, 대화 맥락을 여러 단계 거슬러 추론하지는 않음.
- 신규 발생 계정 조회는 현재 검증용 가상 데이터(모든 세부계정이 매 분기 존재)에서는 항상 "없음"으로 나올 수 있으며, 실제 결산 자료로 교체된 후에야 의미 있는 결과가 나옴.
- 로그인 시 입력하는 이름/사번은 검증되지 않으므로 실제 신원 확인이 아니라 감사 편의를 위한 자기 기입 정보임.
- 다중 사용자 동시 접속 시 성능/동시성은 소규모 본부 인원 기준으로만 검증됨(대규모 동시 접속 미검증).
- 정식 인증(SSO, 사번 기반 권한 검증)은 수정 2차 버전에도 포함되지 않음.

## 11. 향후 로드맵

1. 회사 내부 AI agent API 연동(`llm/internal_agent_client.py` 완성) → 더 자연스러운 질의응답, 복잡한 질문 처리.
2. 질문 파서(NLU)를 내부 AI agent 기반으로 고도화하여 지금의 규칙 기반 파서로는 어려운 조건문·복합 질문("올해 상반기 중 가장 높았던 분기는?", "왜 늘었어?")을 지원.
3. 사번/부서 기반 접근 제어로 인증 방식 강화(현재는 이름/사번을 검증 없이 자기 기입만 받음).
4. 미해결 질문 로그 뷰어를 발전시켜, 자주 실패하는 표현 패턴을 자동으로 집계해 alias/키워드 테이블 개선에 활용.
5. 법인 수가 늘어나거나 계정트리가 더 깊어질 경우를 대비한 브레이크다운 화면(그래프 표시 개수 등) 개선 검토.
6. 여러 계정을 한 화면에 나란히 비교하는 소형 다중 차트(small multiples) 지원 검토.

## 12. 리스크 및 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| 앱 실행 PC/터미널 종료 시 서비스 중단 | 본부 전체 접속 불가 | 상시 켜둘 PC 지정 또는 필요 시에만 담당자가 구동 |
| 공용 비밀번호 유출 | 결산 데이터 노출 | 주기적 비밀번호 변경, 필요 시 인증 방식 고도화 |
| 실제 결산 파일 포맷(법인 구분, 계정트리 깊이 등)이 가정과 다름 | 데이터 적재 실패/누락 | ingest.py의 컬럼/파일명 인식 규칙 조정 |
| 줄임말/유사 표현·오타를 다른 계정으로 잘못 매칭 | 잘못된 수치 응답 | nlu.py의 별칭(alias) 테이블 및 매칭 우선순위·오타 허용 임계값 조정, 응답에 항상 계정명/법인명을 명시해 오인식을 바로 알아챌 수 있게 함 |
| 법인 수가 적은 계정(자료 부족)에서 이상치 탐지 통계가 왜곡됨 | 잘못된 이상치 경고 | 표본 3개 미만이면 이상치 탐지 결과를 내지 않도록 처리 |
| 로그인 시 이름/사번 자기 기입을 신뢰 → 다른 사람 이름으로 기입 가능 | 감사 로그의 신뢰도 저하 | 정식 사번 인증 도입 전까지는 감사 보조 정보로만 사용한다고 안내 |
| 포트가 외부에 노출되는 네트워크 설정 오류 | 데이터 외부 유출 위험 | 방화벽/공유기 설정 점검, 사내망 대역만 허용 |
